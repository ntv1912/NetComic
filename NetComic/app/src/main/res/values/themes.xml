<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NetComic" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NetComic" parent="Base.Theme.NetComic" />
    <!-- Light Mode -->
    <!-- Nền màn hình nếu Light mode Bật-->
    <style name="SCREEN">
        <item name="android:background">#FFF</item>
    </style>

    <!-- Nền chữ nếu Light mode Bật -->
    <style name="TEXT">
        <item name="android:textColor">#000</item>
    </style>

    <!-- Màu nút bấm nếu Light mode Bật -->
    <style name="BUTTON">
        <item name="android:background">#ff0099cc</item>
        <item name="android:textColor">#FFF</item>
    </style>
</resources>